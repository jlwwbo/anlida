# 安粒达

两页静态站，无构建步骤、无依赖。

| 页 | 是什么 | 取材 |
|---|---|---|
| `index.html` | **Landing** —— 问题 → 三章痛点 → 宣言 → 四种失效模式 → 六个动作 → 界面 → 收尾 | 《19-安粒达白皮书》v1.7 |
| `structure.html` | **交互结构图** —— 19 个零件的白模，可悬停、可拆开、可看时序 | 《13-破板取药机构》v2.9、《17-破板力学模型》v1.0 |

版面仿《常宁居》（`~/Documents/gks`）的 landing 结构：刊头 → 全幅问题章节 → 暗带宣言 → 数据 → 步骤 → 收尾 CTA。

```bash
npm run dev                     # = node serve.mjs，零依赖，默认 8732 端口
open http://localhost:8732
```

**没有后端。** 整站是 `index.html` + 三个 ES module + vendored 的 three，
扔进任何静态托管（Vercel / GitHub Pages / 一个 nginx 目录）就能跑。
`serve.mjs` 只是本地预览用的——ES module + importmap 在 `file://` 下会被 CORS 拦，
必须走 HTTP。`npm test` 跑它的路径越界自检。

## 怎么看

| 操作 | 看到什么 |
|---|---|
| 拖拽 / 滚轮 | 转视角、推拉 |
| 悬停零件 | 右上卡片给名称、关键数、状态（已定 / 待验证 / 待设计） |
| 点击零件 | 钉住卡片；再点空白处松开 |
| 左下六段 | 一次取药的时序，点任一段跳过去 |
| 底部「拆开」 | 爆炸图；「外壳」收掉壳体 |

手机上是另一套交互（触屏没有悬停）：

| 操作 | 看到什么 |
|---|---|
| 单指拖 | 转视角；双指缩放 |
| 点零件 | 底部抽屉升起给卡片，同时画面自动上移，被选中的零件不会被抽屉挡住 |
| 点空白 | 收起抽屉 |
| 底部「零件」 | 抽屉里列出全部 19 个零件 |
| 相机 | 按视口长宽比自动退距，竖屏也整机在画内 |

## 结构

```
index.html              landing 骨架（文案全部由 JS 注入）
structure.html          结构图骨架
css/tokens.css          设计令牌（含深色），两页共用
css/landing.css         landing 版面
css/structure.css       结构图版面
src/landing-content.js  landing 的全部文案与数据 —— 改说法只动这个文件
src/landing.js          文案注入、滚动进场、失效模式图、hero 的 3D
src/parts.js            19 个零件 + 六段时序 ——《13》的文字在这里
src/machine.js          几何装配（单位 mm，直接用《13》3.3 与 4.4 M1 的尺寸）+ pose(t)
src/main.js             结构图页：渲染、拾取高亮、爆炸、时序播放、抽屉
serve.mjs               本地预览用的零依赖静态服务
vendor/three/           three r180，已 vendored，离线可跑
```

两页共用 `machine.js`：landing 的 hero 就是同一台机器，慢速摆动 + 循环播放那六个动作，滚出视口即停。

## 设计口径

- **纸为底，机器是唯一的白，药是唯一的饱和色。** 画布透明，底色由 CSS 画；地面只接阴影（`ShadowMaterial`）。
- 字：标题 Noto Serif SC，一切数字与标号 Space Mono（沿用 `gks` 的口径），正文走系统中文黑体。
- 状态只有三档，用形而不只用色：实心 = 已定，琥珀 = 待验证，空心环 = 待设计。
- 20 个槽里故意放了四种板型（2×5 片剂 / 1×7 胶囊 / 3×4 小片 / 长条片），这是「不同板都能插进同一个槽」那句话的视觉证据。

## 与文档的对应

几何与数值全部来自《13》，不在这里另立一套：

| 这里 | 出处 |
|---|---|
| 380 × 250 × 180，槽距 17 mm × 20 | 13 §3.3、§4.4 M1、§4.5 ② |
| 无夹爪：薄推杆 + 磁铁，推出行程兼列对位 | 13 §4.4 M1 |
| 马蹄支撑座 C 形、缺口朝下、净空 14 mm | 13 §S1、§4.8 D |
| 60 N 力流闭合在 C 形滑车内部 | 13 §3.1 |
| 双侧相机 + 漫射频闪，不停车 | 13 §4.8 B |
| 六段时序与 4.1 s/粒 | 13 §五、§5.5 |
| 70 N 作动器、6 mm² 接触面积判据、11 m/s 出膛上界 | 17 §四、§五 |

动画为看得清而放慢，约为真实节拍的两倍。
